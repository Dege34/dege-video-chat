[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "Dege"
version = "1.0.0"
description = "Dege Video Chat Framework"

[project.scripts]
dege = "Dege.cli:main"

[tool.setuptools.packages.find]
include = ["Dege*"]
